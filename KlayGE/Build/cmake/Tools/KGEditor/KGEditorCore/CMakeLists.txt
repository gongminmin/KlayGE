SET(SOURCE_FILES 
	${KLAYGE_PROJECT_DIR}/Tools/src/KGEditor/KGEditorCore/KGEditorCore.cpp
)

SET(HEADER_FILES 
	${KLAYGE_PROJECT_DIR}/Tools/src/KGEditor/KGEditorCore/Common.hpp
	${KLAYGE_PROJECT_DIR}/Tools/src/KGEditor/KGEditorCore/KGEditorCore.hpp
	${KLAYGE_PROJECT_DIR}/Tools/src/KGEditor/KGEditorCore/PreDeclare.hpp
)

SET(EFFECT_FILES 
	${KLAYGE_PROJECT_DIR}/Tools/media/KGEditor/KGEditor.fxml
	${KLAYGE_PROJECT_DIR}/Tools/media/KGEditor/KGEditorNoSkinning.fxml
	${KLAYGE_PROJECT_DIR}/Tools/media/KGEditor/KGEditorSkinning64.fxml
	${KLAYGE_PROJECT_DIR}/Tools/media/KGEditor/KGEditorSkinning128.fxml
	${KLAYGE_PROJECT_DIR}/Tools/media/KGEditor/MVUtil.fxml
)

SOURCE_GROUP("Source Files" FILES ${SOURCE_FILES})
SOURCE_GROUP("Header Files" FILES ${HEADER_FILES})
SOURCE_GROUP("Resource Files" FILES ${RESOURCE_FILES})
SOURCE_GROUP("Effect Files" FILES ${EFFECT_FILES})
SOURCE_GROUP("Post Processors" FILES ${POST_PROCESSORS})
SOURCE_GROUP("UI Files" FILES ${UI_FILES})

ADD_DEFINITIONS(-DKLAYGE_KGEDITOR_CORE_SOURCE)

INCLUDE_DIRECTORIES(${Boost_INCLUDE_DIR})
INCLUDE_DIRECTORIES(${KLAYGE_PROJECT_DIR}/../KFL/include)
INCLUDE_DIRECTORIES(${KLAYGE_PROJECT_DIR}/Core/Include)
INCLUDE_DIRECTORIES(${EXTRA_INCLUDE_DIRS})
LINK_DIRECTORIES(${Boost_LIBRARY_DIR})
LINK_DIRECTORIES(${KLAYGE_PROJECT_DIR}/../KFL/lib/${KLAYGE_PLATFORM_NAME})
LINK_DIRECTORIES(${KLAYGE_PROJECT_DIR}/lib/${KLAYGE_PLATFORM_NAME})
LINK_DIRECTORIES(${EXTRA_LINKED_DIRS})

SET(KGEDITOR_CORE_NAME KGEditorCore)

ADD_LIBRARY(${KGEDITOR_CORE_NAME} SHARED ${SOURCE_FILES} ${HEADER_FILES} ${RESOURCE_FILES} ${EFFECT_FILES} ${POST_PROCESSORS} ${UI_FILES})
ADD_DEPENDENCIES(${KGEDITOR_CORE_NAME} AllInEngine)

SET_TARGET_PROPERTIES(${KGEDITOR_CORE_NAME} PROPERTIES
	ARCHIVE_OUTPUT_DIRECTORY ${KGEDITOR_CORE_LIB_OUTPUT_DIR}
	ARCHIVE_OUTPUT_DIRECTORY_DEBUG ${KGEDITOR_CORE_LIB_OUTPUT_DIR}
	ARCHIVE_OUTPUT_DIRECTORY_RELEASE ${KGEDITOR_CORE_LIB_OUTPUT_DIR}
	ARCHIVE_OUTPUT_DIRECTORY_RELWITHDEBINFO ${KGEDITOR_CORE_LIB_OUTPUT_DIR}
	ARCHIVE_OUTPUT_DIRECTORY_MINSIZEREL ${KGEDITOR_CORE_LIB_OUTPUT_DIR}
	PROJECT_LABEL ${KGEDITOR_CORE_NAME}
	DEBUG_POSTFIX ${CMAKE_DEBUG_POSTFIX}
	OUTPUT_NAME ${KGEDITOR_CORE_NAME}
)

IF(NOT KLAYGE_COMPILER_MSVC)
	SET(EXTRA_LINKED_LIBRARIES ${EXTRA_LINKED_LIBRARIES}
		debug KlayGE_Core${KLAYGE_OUTPUT_SUFFIX}_d optimized KlayGE_Core${KLAYGE_OUTPUT_SUFFIX}
		debug KFL${KLAYGE_OUTPUT_SUFFIX}_d optimized KFL${KLAYGE_OUTPUT_SUFFIX})
ENDIF()

TARGET_LINK_LIBRARIES(${KGEDITOR_CORE_NAME} ${EXTRA_LINKED_LIBRARIES})


ADD_POST_BUILD(${KGEDITOR_CORE_NAME} "")

INSTALL(TARGETS ${KGEDITOR_CORE_NAME}
	RUNTIME DESTINATION ${KLAYGE_BIN_DIR}
	LIBRARY DESTINATION ${KLAYGE_BIN_DIR}
	ARCHIVE DESTINATION ${KLAYGE_OUTPUT_DIR}
)

SET_TARGET_PROPERTIES(${KGEDITOR_CORE_NAME} PROPERTIES FOLDER "Tools/KGEditor")
