SET(SOURCE_FILES 
	${KLAYGE_PROJECT_DIR}/Tools/src/MeshMLViewer/MeshMLViewerCore/MeshMLViewerCore.cpp
	${KLAYGE_PROJECT_DIR}/Tools/src/MeshMLViewer/MeshMLViewerCore/Model.cpp
)

SET(HEADER_FILES 
	${KLAYGE_PROJECT_DIR}/Tools/src/MeshMLViewer/MeshMLViewerCore/MeshMLViewerCore.hpp
	${KLAYGE_PROJECT_DIR}/Tools/src/MeshMLViewer/MeshMLViewerCore/Model.hpp
)

SET(EFFECT_FILES 
	${KLAYGE_PROJECT_DIR}/Tools/media/MeshMLViewer/MeshMLViewer.fxml
	${KLAYGE_PROJECT_DIR}/Tools/media/MeshMLViewer/MeshMLViewerNoSkinning.fxml
	${KLAYGE_PROJECT_DIR}/Tools/media/MeshMLViewer/MeshMLViewerSkinning64.fxml
	${KLAYGE_PROJECT_DIR}/Tools/media/MeshMLViewer/MeshMLViewerSkinning128.fxml
	${KLAYGE_PROJECT_DIR}/Tools/media/MeshMLViewer/MVUtil.fxml
)

SOURCE_GROUP("Source Files" FILES ${SOURCE_FILES})
SOURCE_GROUP("Header Files" FILES ${HEADER_FILES})
SOURCE_GROUP("Resource Files" FILES ${RESOURCE_FILES})
SOURCE_GROUP("Effect Files" FILES ${EFFECT_FILES})
SOURCE_GROUP("Post Processors" FILES ${POST_PROCESSORS})
SOURCE_GROUP("UI Files" FILES ${UI_FILES})

ADD_DEFINITIONS(-DKLAYGE_MESHML_VIEWER_CORE_SOURCE)

INCLUDE_DIRECTORIES(${Boost_INCLUDE_DIR})
INCLUDE_DIRECTORIES(${KLAYGE_PROJECT_DIR}/../KFL/include)
INCLUDE_DIRECTORIES(${KLAYGE_PROJECT_DIR}/Core/Include)
INCLUDE_DIRECTORIES(${EXTRA_INCLUDE_DIRS})
LINK_DIRECTORIES(${Boost_LIBRARY_DIR})
LINK_DIRECTORIES(${KLAYGE_PROJECT_DIR}/../KFL/lib/${KLAYGE_PLATFORM_NAME})
LINK_DIRECTORIES(${KLAYGE_PROJECT_DIR}/lib/${KLAYGE_PLATFORM_NAME})
LINK_DIRECTORIES(${EXTRA_LINKED_DIRS})

SET(MESHML_VIEWER_CORE_NAME MeshMLViewerCore)

ADD_LIBRARY(${MESHML_VIEWER_CORE_NAME} SHARED ${SOURCE_FILES} ${HEADER_FILES} ${RESOURCE_FILES} ${EFFECT_FILES} ${POST_PROCESSORS} ${UI_FILES})

SET_TARGET_PROPERTIES(${MESHML_VIEWER_CORE_NAME} PROPERTIES
	ARCHIVE_OUTPUT_DIRECTORY ${MESHML_VIEWER_CORE_LIB_OUTPUT_DIR}
	ARCHIVE_OUTPUT_DIRECTORY_DEBUG ${MESHML_VIEWER_CORE_LIB_OUTPUT_DIR}
	ARCHIVE_OUTPUT_DIRECTORY_RELEASE ${MESHML_VIEWER_CORE_LIB_OUTPUT_DIR}
	ARCHIVE_OUTPUT_DIRECTORY_RELWITHDEBINFO ${MESHML_VIEWER_CORE_LIB_OUTPUT_DIR}
	ARCHIVE_OUTPUT_DIRECTORY_MINSIZEREL ${MESHML_VIEWER_CORE_LIB_OUTPUT_DIR}
	PROJECT_LABEL ${MESHML_VIEWER_CORE_NAME}
	DEBUG_POSTFIX ${CMAKE_DEBUG_POSTFIX}
	OUTPUT_NAME ${MESHML_VIEWER_CORE_NAME}
)

IF(MSVC)
	SET(EXTRA_LINKED_LIBRARIES ${EXTRA_LINKED_LIBRARIES})
ELSE()
	SET(EXTRA_LINKED_LIBRARIES ${EXTRA_LINKED_LIBRARIES}
		debug KlayGE_Core${KLAYGE_OUTPUT_SUFFIX}_d optimized KlayGE_Core${KLAYGE_OUTPUT_SUFFIX}
		debug KFL${KLAYGE_OUTPUT_SUFFIX}_d optimized KFL${KLAYGE_OUTPUT_SUFFIX}
		${Boost_SYSTEM_LIBRARY})
ENDIF()

TARGET_LINK_LIBRARIES(${MESHML_VIEWER_CORE_NAME} ${EXTRA_LINKED_LIBRARIES})


ADD_POST_BUILD(${MESHML_VIEWER_CORE_NAME} "")

INSTALL(TARGETS ${MESHML_VIEWER_CORE_NAME}
	RUNTIME DESTINATION ${KLAYGE_BIN_DIR}
	LIBRARY DESTINATION ${KLAYGE_BIN_DIR}
	ARCHIVE DESTINATION ${MESHML_VIEWER_CORE_LIB_OUTPUT_DIR}
)

SET_TARGET_PROPERTIES(${MESHML_VIEWER_CORE_NAME} PROPERTIES FOLDER "Tools/MeshMLViewer")
