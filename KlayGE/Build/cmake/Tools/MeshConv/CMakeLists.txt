SET(SOURCE_FILES
	${KLAYGE_PROJECT_DIR}/Tools/src/MeshConv/MeshConv.cpp
)

SET(EXTRA_INCLUDE_DIRS ${EXTRA_INCLUDE_DIRS}
		${KLAYGE_PROJECT_DIR}/../External/assimp/include
		${KLAYGE_PROJECT_DIR}/../MeshMLLib/include)

SET(EXTRA_LINKED_DIRS ${EXTRA_LINKED_DIRS}
	${KLAYGE_PROJECT_DIR}/../External/assimp/lib/${KLAYGE_PLATFORM_NAME}
	${KLAYGE_PROJECT_DIR}/../MeshMLLib/lib/${KLAYGE_PLATFORM_NAME})

SET(EXTRA_LINKED_LIBRARIES ${EXTRA_LINKED_LIBRARIES}
	debug assimp${KLAYGE_OUTPUT_SUFFIX}${CMAKE_DEBUG_POSTFIX} optimized assimp${KLAYGE_OUTPUT_SUFFIX}
	debug zlibstatic${KLAYGE_OUTPUT_SUFFIX}${CMAKE_DEBUG_POSTFIX} optimized zlibstatic${KLAYGE_OUTPUT_SUFFIX}
	debug MeshMLLib${KLAYGE_OUTPUT_SUFFIX}${CMAKE_DEBUG_POSTFIX} optimized MeshMLLib${KLAYGE_OUTPUT_SUFFIX})
IF(MSVC)
	SET(EXTRA_LINKED_LIBRARIES ${EXTRA_LINKED_LIBRARIES})
ELSE()
	SET(EXTRA_LINKED_LIBRARIES ${EXTRA_LINKED_LIBRARIES}
		${Boost_PROGRAM_OPTIONS_LIBRARY} ${Boost_FILESYSTEM_LIBRARY} ${Boost_REGEX_LIBRARY})
ENDIF()

SETUP_TOOL(MeshConv)
