SET(LIB_NAME KlayGE_ShowEngine_DShow)

SET(DSHOW_SE_SOURCE_FILES
	${KLAYGE_PROJECT_DIR}/Plugins/Src/Show/DShow/DShowEngine.cpp
	${KLAYGE_PROJECT_DIR}/Plugins/Src/Show/DShow/DShowFactory.cpp
	${KLAYGE_PROJECT_DIR}/Plugins/Src/Show/DShow/DShowVMR9Allocator.cpp
)

SET(DSHOW_SE_HEADER_FILES
	${KLAYGE_PROJECT_DIR}/Plugins/Include/KlayGE/DShow/DShow.hpp
	${KLAYGE_PROJECT_DIR}/Plugins/Include/KlayGE/DShow/DShowFactory.hpp
	${KLAYGE_PROJECT_DIR}/Plugins/Include/KlayGE/DShow/DShowVMR9Allocator.hpp
)

SOURCE_GROUP("Source Files" FILES ${DSHOW_SE_SOURCE_FILES})
SOURCE_GROUP("Header Files" FILES ${DSHOW_SE_HEADER_FILES})

ADD_DEFINITIONS(-DKLAYGE_BUILD_DLL -DKLAYGE_DSHOW_SE_SOURCE)

INCLUDE_DIRECTORIES(${KLAYGE_PROJECT_DIR}/../External/boost)
IF(MSVC)
	IF(MSVC_VERSION LESS 1699)
		INCLUDE_DIRECTORIES(${KLAYGE_PROJECT_DIR}/../External/DXSDK/Include)
	ENDIF()
ELSE()
	INCLUDE_DIRECTORIES(${KLAYGE_PROJECT_DIR}/../External/DXSDK/Include)
ENDIF()
INCLUDE_DIRECTORIES(${KLAYGE_PROJECT_DIR}/Core/Include)
INCLUDE_DIRECTORIES(${KLAYGE_PROJECT_DIR}/Plugins/Include)
LINK_DIRECTORIES(${KLAYGE_PROJECT_DIR}/../External/boost/lib_${KLAYGE_COMPILER_NAME}${KLAYGE_COMPILER_VERSION}_${KLAYGE_ARCH_NAME}/lib)
LINK_DIRECTORIES(${KLAYGE_PROJECT_DIR}/lib/${KLAYGE_PLATFORM_NAME})

ADD_LIBRARY(${LIB_NAME} SHARED
    ${DSHOW_SE_SOURCE_FILES} ${DSHOW_SE_HEADER_FILES}
)
ADD_DEPENDENCIES(${LIB_NAME} ${KLAYGE_CORELIB_NAME})

SET_TARGET_PROPERTIES(${LIB_NAME} PROPERTIES PROJECT_LABEL "${LIB_NAME}")
SET_TARGET_PROPERTIES(${LIB_NAME} PROPERTIES DEBUG_POSTFIX "${CMAKE_DEBUG_POSTFIX}")
SET_TARGET_PROPERTIES(${LIB_NAME} PROPERTIES OUTPUT_NAME "${LIB_NAME}${KLAYGE_NAME_POSTFIX}")

IF(MSVC)
	# hack to get around the "Debug" and "Release" directories cmake tries to add on Windows
	SET_TARGET_PROPERTIES(${LIB_NAME} PROPERTIES IMPORT_PREFIX "../")

	SET(EXTRA_LINKED_LIBRARIES "")
ELSE()
	SET(EXTRA_LINKED_LIBRARIES boost_system)
ENDIF()

ADD_PRECOMPILED_HEADER(${LIB_NAME} "KlayGE/KlayGE.hpp" "${KLAYGE_PROJECT_DIR}/Plugins/Src/Show/DShow/DShowFactory.cpp")

SET_TARGET_PROPERTIES(${LIB_NAME} PROPERTIES
	ARCHIVE_OUTPUT_DIRECTORY ${KLAYGE_OUTPUT_DIR}
	PROJECT_LABEL ${LIB_NAME}
	DEBUG_POSTFIX ${CMAKE_DEBUG_POSTFIX}
	OUTPUT_NAME ${LIB_NAME}${KLAYGE_NAME_POSTFIX}
)


ADD_POST_BUILD(${LIB_NAME} "Show")

 
INSTALL(TARGETS ${LIB_NAME}
	RUNTIME DESTINATION ${KLAYGE_BIN_DIR}/Show
	LIBRARY DESTINATION ${KLAYGE_BIN_DIR}/Show
	ARCHIVE DESTINATION ${KLAYGE_OUTPUT_DIR}
)

SET_TARGET_PROPERTIES(${LIB_NAME} PROPERTIES FOLDER "Show System")
