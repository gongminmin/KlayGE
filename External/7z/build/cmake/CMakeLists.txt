PROJECT(7z)

CMAKE_MINIMUM_REQUIRED(VERSION 2.8.6)

IF(COMMAND cmake_policy)
	CMAKE_POLICY(SET CMP0003 NEW)
ENDIF(COMMAND cmake_policy)

SET(SEVENZIP_MAJOR_VERSION 9)
SET(SEVENZIP_MINOR_VERSION 20)
SET(SEVENZIP_VERSION ${SEVENZIP_MAJOR_VERSION}.${SEVENZIP_MINOR_VERSION})

FIND_PACKAGE(PythonInterp)

SET(SEVENZIP_PROJECT_DIR "${CMAKE_HOME_DIRECTORY}/../..")
SET(KLAYGE_ROOT_DIR "${SEVENZIP_PROJECT_DIR}/../..")

INCLUDE(${KLAYGE_ROOT_DIR}/cmake/Common.cmake)
INCLUDE(${KLAYGE_ROOT_DIR}/cmake/Platform.cmake)
INCLUDE(${KLAYGE_ROOT_DIR}/cmake/Compiler.cmake)

SET_PROPERTY(GLOBAL PROPERTY USE_FOLDERS ON)

SET(SEVENZIP_OUTPUT_DIR "${SEVENZIP_PROJECT_DIR}/lib/${KLAYGE_PLATFORM_NAME}")
SET(KLAYGE_BIN_DIR "${KLAYGE_ROOT_DIR}/KlayGE/bin/${KLAYGE_PLATFORM_NAME}")
SET(REL_PATH "External/Downloads/7z${SEVENZIP_MAJOR_VERSION}${SEVENZIP_MINOR_VERSION}.tar.bz2")
SET(DST_PATH "${KLAYGE_ROOT_DIR}/External/7z/DOC/readme.txt")
SET(SEVENZIP_PATH "${KLAYGE_ROOT_DIR}/External/Downloads/${KLAYGE_PLATFORM_NAME}/7z")
SET(PATCH_PATH "${KLAYGE_ROOT_DIR}/External/Downloads/patch.py")

IF(KLAYGE_PLATFORM_WINDOWS)
	DOWNLOAD_FILE("External/Downloads/${KLAYGE_PLATFORM_NAME}/7z.exe")
	DOWNLOAD_FILE("External/Downloads/${KLAYGE_PLATFORM_NAME}/7z.dll")
ELSE()
	DOWNLOAD_FILE("External/Downloads/${KLAYGE_PLATFORM_NAME}/7z")
	DOWNLOAD_FILE("External/Downloads/${KLAYGE_PLATFORM_NAME}/7z.so")
	FILE(COPY SEVENZIP_PATH DESTINATION "${KLAYGE_ROOT_DIR}/External/Downloads")
	FILE(COPY "${KLAYGE_ROOT_DIR}/External/Downloads" DESTINATION "${KLAYGE_ROOT_DIR}/External/Downloads/${KLAYGE_PLATFORM_NAME}" FILE_PERMISSIONS OWNER_READ OWNER_WRITE OWNER_EXECUTE)
ENDIF()
DOWNLOAD_FILE("External/Downloads/patch.py")
DOWNLOAD_FILE_URL(${REL_PATH} "http://www.7-zip.org/a/7z${SEVENZIP_MAJOR_VERSION}${SEVENZIP_MINOR_VERSION}.tar.bz2")

IF(EXISTS ${DST_PATH})
	SET(SEVENZIP_VERSION_STR 0)
	FILE(STRINGS ${DST_PATH} SEVENZIP_VERSION_STR REGEX "7-Zip ")
	STRING(FIND "${SEVENZIP_VERSION_STR}" "${SEVENZIP_VERSION}" POS)
	IF(NOT POS EQUAL 6)
		FILE(REMOVE ${DST_PATH})
	ENDIF()
ENDIF()

ADD_CUSTOM_COMMAND(OUTPUT ${DST_PATH}
	COMMAND ${SEVENZIP_PATH} x -y ${KLAYGE_ROOT_DIR}/${REL_PATH}
	COMMAND ${SEVENZIP_PATH} x -y 7z${SEVENZIP_MAJOR_VERSION}${SEVENZIP_MINOR_VERSION}.tar
	COMMAND ${CMAKE_COMMAND} -E remove 7z${SEVENZIP_MAJOR_VERSION}${SEVENZIP_MINOR_VERSION}.tar
	COMMAND ${PYTHON_EXECUTABLE} ${PATCH_PATH} -p0 Patches/ForOSXIOSAndroidAndWinRT.patch
	COMMAND ${CMAKE_COMMAND} -E touch_nocreate ${DST_PATH}
	WORKING_DIRECTORY ${KLAYGE_ROOT_DIR}/External/7z)
ADD_CUSTOM_TARGET(7z_download ALL DEPENDS ${DST_PATH})

IF(MSVC)
	SET(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} /W3 /WX-")
	SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} /W3 /WX-")
ENDIF()

ADD_SUBDIRECTORY(7zxa)
ADD_SUBDIRECTORY(LZMA)
