PROJECT(rapidxml NONE)

CMAKE_MINIMUM_REQUIRED(VERSION 2.8.6)

IF(COMMAND cmake_policy)
	CMAKE_POLICY(SET CMP0003 NEW)
ENDIF(COMMAND cmake_policy)

SET(RAPIDXML_MAJOR_VERSION 1)
SET(RAPIDXML_MINOR_VERSION 13)
SET(RAPIDXML_VERSION ${RAPIDXML_MAJOR_VERSION}.${RAPIDXML_MINOR_VERSION})

SET(RAPIDXML_DIR "${CMAKE_CURRENT_SOURCE_DIR}/../..")
SET(KLAYGE_ROOT_DIR "${RAPIDXML_DIR}/../..")

INCLUDE(${KLAYGE_ROOT_DIR}/cmake/Common.cmake)
INCLUDE(${KLAYGE_ROOT_DIR}/cmake/Platform.cmake)
INCLUDE(${KLAYGE_ROOT_DIR}/External/Build/CMake/ExternalCommon.cmake)

SET(KLAYGE_BIN_DIR "${RAPIDXML_DIR}/../../KlayGE/bin/${KLAYGE_PLATFORM_NAME}")
SET(REL_PATH "External/Downloads/rapidxml-${RAPIDXML_VERSION}.patched.7z")
SET(SEVENZIP_PATH "${KLAYGE_ROOT_DIR}/External/Downloads/${KLAYGE_HOST_PLATFORM_NAME}/7z")
SET(PACKAGE_COMMIT_ID "9f3de43e07f09990641aa5855d5cd9d17646120e")
SET(PACKAGE_FILE_ID "8747b2f3f39470950405c82413b3bc8551f37777")

DOWNLOAD_PACKAGE("rapidxml" ${REL_PATH} ${PACKAGE_COMMIT_ID} ${PACKAGE_FILE_ID} "rapidxml.hpp")
