SET(LIB_NAME vorbisfile)

SET(LIBVORBISFILE_SOURCE_FILES
	${VORBIS_PROJECT_DIR}/lib/vorbisfile.c
)
SET(LIBVORBISFILE_HEADER_FILES
	${VORBIS_PROJECT_DIR}/include/vorbis/vorbisfile.h
)
SOURCE_GROUP("Source Files" FILES ${LIBVORBISFILE_SOURCE_FILES})
SOURCE_GROUP("Header Files" FILES ${LIBVORBISFILE_HEADER_FILES})

ADD_DEFINITIONS(-DLIBVORBIS_EXPORTS)
IF(KLAYGE_WITH_WINRT)
	ADD_DEFINITIONS(-ZW)
	SET(CMAKE_C_COMPILER ${CMAKE_CXX_COMPILER})
	SET_SOURCE_FILES_PROPERTIES(${LIBVORBISFILE_SOURCE_FILES} PROPERTIES LANGUAGE CXX)
ENDIF()

INCLUDE_DIRECTORIES(${VORBIS_PROJECT_DIR}/include)
INCLUDE_DIRECTORIES(${VORBIS_PROJECT_DIR}/../libogg/include)
ADD_LIBRARY(${LIB_NAME} STATIC
	${LIBVORBISFILE_SOURCE_FILES} ${LIBVORBISFILE_HEADER_FILES}
)

IF(MSVC)
	SET(LIB_OUTPUT_NAME "lib${LIB_NAME}_static")
ELSE()
	SET(LIB_OUTPUT_NAME ${LIB_NAME}_static)
ENDIF()

SET_TARGET_PROPERTIES(${LIB_NAME} PROPERTIES
	ARCHIVE_OUTPUT_DIRECTORY ${VORBIS_OUTPUT_DIR}
	ARCHIVE_OUTPUT_DIRECTORY_DEBUG ${VORBIS_OUTPUT_DIR}
	ARCHIVE_OUTPUT_DIRECTORY_RELEASE ${VORBIS_OUTPUT_DIR}
	ARCHIVE_OUTPUT_DIRECTORY_RELWITHDEBINFO ${VORBIS_OUTPUT_DIR}
	ARCHIVE_OUTPUT_DIRECTORY_MINSIZEREL ${VORBIS_OUTPUT_DIR}
	PROJECT_LABEL ${LIB_NAME}
	DEBUG_POSTFIX ${CMAKE_DEBUG_POSTFIX}
	OUTPUT_NAME ${LIB_OUTPUT_NAME}
)
IF(KLAYGE_WITH_WINRT)
	SET_TARGET_PROPERTIES(${LIB_NAME} PROPERTIES VS_WINRT_EXTENSIONS TRUE)
ENDIF()

TARGET_LINK_LIBRARIES(${LIB_NAME})