PROJECT(libogg)

CMAKE_MINIMUM_REQUIRED(VERSION 2.8.6)

IF(COMMAND cmake_policy)
	CMAKE_POLICY(SET CMP0003 NEW)
ENDIF(COMMAND cmake_policy)

SET(LIBOGG_MAJOR_VERSION 1)
SET(LIBOGG_MINOR_VERSION 3)
SET(LIBOGG_PATCH_VERSION 0)
SET(LIBOGG_VERSION ${LIBOGG_MAJOR_VERSION}.${LIBOGG_MINOR_VERSION}.${LIBOGG_PATCH_VERSION})

FIND_PACKAGE(PythonInterp)

SET(LIBOGG_PROJECT_DIR "${CMAKE_CURRENT_SOURCE_DIR}/../..")
SET(KLAYGE_ROOT_DIR "${LIBOGG_PROJECT_DIR}/../..")

INCLUDE(${KLAYGE_ROOT_DIR}/cmake/Common.cmake)
INCLUDE(${KLAYGE_ROOT_DIR}/cmake/Platform.cmake)
INCLUDE(${KLAYGE_ROOT_DIR}/cmake/Compiler.cmake)

SET_PROPERTY(GLOBAL PROPERTY USE_FOLDERS ON)

SET(LIBOGG_OUTPUT_DIR "${LIBOGG_PROJECT_DIR}/lib/${KLAYGE_PLATFORM_NAME}")
SET(REL_PATH "External/Downloads/libogg-${LIBOGG_VERSION}.patched.7z")
SET(DST_PATH "${KLAYGE_ROOT_DIR}/External/libogg/CHANGES")
SET(SEVENZIP_PATH "${KLAYGE_ROOT_DIR}/External/Downloads/${KLAYGE_PLATFORM_NAME}/7z")

SET(REGEN FALSE)
IF(EXISTS ${DST_PATH})
	SET(LIBOGG_VERSION_STR 0)
	FILE(STRINGS ${DST_PATH} LIBOGG_VERSION_STR REGEX "Version ")
	STRING(FIND "${LIBOGG_VERSION_STR}" "Version ${LIBOGG_VERSION}" POS)
	IF(NOT POS EQUAL 0)
		SET(REGEN TRUE)
	ENDIF()
ELSE()
	SET(REGEN TRUE)
ENDIF()

IF(REGEN)
	IF(KLAYGE_PLATFORM_WINDOWS)
		DOWNLOAD_FILE("External/Downloads/${KLAYGE_PLATFORM_NAME}/7z.exe" "cbda47a1678ce70b6720856736100979d469e159")
		DOWNLOAD_FILE("External/Downloads/${KLAYGE_PLATFORM_NAME}/7z.dll" "cbda47a1678ce70b6720856736100979d469e159")
	ELSE()
		DOWNLOAD_FILE("External/Downloads/${KLAYGE_PLATFORM_NAME}/7z" "cbda47a1678ce70b6720856736100979d469e159")
		DOWNLOAD_FILE("External/Downloads/${KLAYGE_PLATFORM_NAME}/7z.so" "cbda47a1678ce70b6720856736100979d469e159")
		FILE(COPY ${SEVENZIP_PATH} DESTINATION "${KLAYGE_ROOT_DIR}/External/Downloads")
		FILE(COPY "${KLAYGE_ROOT_DIR}/External/Downloads/7z" DESTINATION "${KLAYGE_ROOT_DIR}/External/Downloads/${KLAYGE_PLATFORM_NAME}" FILE_PERMISSIONS OWNER_READ OWNER_WRITE OWNER_EXECUTE)
	ENDIF()
	DOWNLOAD_FILE(${REL_PATH} "9f3de43e07f09990641aa5855d5cd9d17646120e")

	EXECUTE_PROCESS(COMMAND "${SEVENZIP_PATH}" "x" "-y" "${KLAYGE_ROOT_DIR}/${REL_PATH}" WORKING_DIRECTORY "${KLAYGE_ROOT_DIR}/External/libogg")
ENDIF()


# The project settings

SET(LIB_NAME ogg)

SET(LIBOGG_SOURCE_FILES
	${LIBOGG_PROJECT_DIR}/src/bitwise.c
	${LIBOGG_PROJECT_DIR}/src/framing.c
)
SET(LIBOGG_HEADER_FILES
	${LIBOGG_PROJECT_DIR}/include/ogg/ogg.h
	${LIBOGG_PROJECT_DIR}/include/ogg/os_types.h
)
SOURCE_GROUP("Source Files" FILES ${LIBOGG_SOURCE_FILES})
SOURCE_GROUP("Header Files" FILES ${LIBOGG_HEADER_FILES})

ADD_DEFINITIONS(-DLIBOGG_EXPORTS)
IF(KLAYGE_PLATFORM_WINDOWS_RUNTIME)
	SET(CMAKE_C_COMPILER ${CMAKE_CXX_COMPILER})
	SET_SOURCE_FILES_PROPERTIES(${LIBOGG_SOURCE_FILES} PROPERTIES LANGUAGE CXX)
ENDIF()

INCLUDE_DIRECTORIES(${LIBOGG_PROJECT_DIR}/include)
ADD_LIBRARY(${LIB_NAME} STATIC
	${LIBOGG_SOURCE_FILES} ${LIBOGG_HEADER_FILES}
)

SET(LIB_OUTPUT_NAME ${LIB_NAME}_static)

SET_TARGET_PROPERTIES(${LIB_NAME} PROPERTIES
	ARCHIVE_OUTPUT_DIRECTORY ${LIBOGG_OUTPUT_DIR}
	ARCHIVE_OUTPUT_DIRECTORY_DEBUG ${LIBOGG_OUTPUT_DIR}
	ARCHIVE_OUTPUT_DIRECTORY_RELEASE ${LIBOGG_OUTPUT_DIR}
	ARCHIVE_OUTPUT_DIRECTORY_RELWITHDEBINFO ${LIBOGG_OUTPUT_DIR}
	ARCHIVE_OUTPUT_DIRECTORY_MINSIZEREL ${LIBOGG_OUTPUT_DIR}
	PROJECT_LABEL ${LIB_NAME}
	DEBUG_POSTFIX ${CMAKE_DEBUG_POSTFIX}
	OUTPUT_NAME ${LIB_OUTPUT_NAME}${KLAYGE_OUTPUT_SUFFIX}
)

TARGET_LINK_LIBRARIES(${LIB_NAME})
